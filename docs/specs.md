# AI-Driven Reverse Scheduler

計画を余裕を持って、かつ動的に管理・改組するアプリケーション。

## Concept

よくあるのが、完璧に逆算した計画を立てても、見積もりの甘さや突発的なタスクによって、必ずスケジュールは狂う。
既存のツールは「遅れたタスクを空き時間に詰め直す」という機能を搭載しているだけに過ぎない。
ここで、本システムは「間に合わないなら、スコープ自体を削る」という決断を、AIが自律的に提案・サポートする。

   User
    │
    ▼
┌──────────────────────┐
│   Scope Optimizer    │
│                      │
│  Buffer Management   │
│  Urgency Scoring     │
│  Just-in-Time Prompt │
│  AI Scope Triage     │
└──────────┬───────────┘
           │
     ┌─────┼─────┐
     ▼     ▼     ▼
   Drop Simplify Keep
     │     │     │
     └─────┼─────┘
           ▼
    Project Landing

## Goal

単なる「予定の再配置」ではなく、コアとなる要素を中心に必ずプロジェクトを着地させることを目的とする。

## Core Engine

システムは、スケジュールの限界を検知し、タスクの要件を意味論的に縮小する。

*   **Buffer Management**
    タスクの総工数から最初から20%をバッファとして差し引き、「実質締切」をシステム内部で保持する。
*   **Dynamic Urgency**
    残工数と残日数の比率から「今日サボった場合の明日の負荷」を計算し、タスク状態をGreen/Yellow/Redで視覚化する。
*   **Just-in-Time Prompting**
    初期は「タスク名」と「締切」のみ。進捗がYellow（警告）になったタイミングで初めて、AIがスケジュールの立て直しに必要な情報（重要度・目的）をユーザーに要求する。**深刻度に応じてグラデーションをしたい**
*   **AI Scope Triage**
    スケジュールがRed（危険）に突入した際、LLMが「なぜこの作業を削っても目的は達成できるのか」という論理的理由と共に、タスクの完全削除や簡略化を提案する。
*   **評価と学習**
    タスク終了後にユーザに評価させ，工数計算をより正確に
## Status

🚧 Early-stage / MVP Development
現在はプロトタイプ実装フェーズ。依存関係の描画や外部カレンダー連携を削ぎ落とし、AIによる「トリアージ提案」のコアロジック検証に集中している。
）