# AI-Driven Reverse Scheduler システム要件定義書 (v1)
## システム概要
### システム名称
AI-Driven Reverse Scheduler（AI駆動型 逆算スケジューラー）

### システム目的
AI-Driven Reverse Schedulerは，ハッカソン，日々の課題などの短期間プロジェクトにおいて**絶対に期日を守る**ことに特化したタスク管理・進行支援システムである．

従来の積み上げ型ではなく，最終目標と絶対期日からの逆算（CCPM概念の応用）によりプロジェクトのバッファ（余裕）を可視化する．遅延やバッファ枯渇を検知した際は，AIが感情を持たないPMとして「タスクの削減（Drop）」や「要件の簡略化」を強行提案（トリアージ）し，破綻を防ぐセルフリカバリー機能を提供する．

### システム利用者
プロジェクト開発者（ハッカソン参加者，エンジニア，PM等）
*****

## 業務要件
### プロジェクト初期設定（Intake）
利用者は目標と絶対期日を入力し，スケジュールの起点を作成できること．

#### 要求事項
- 最終目標（例：プレゼン資料の完成）をテキストで入力できること

- プロジェクトの絶対期日（Deadline）を設定できること

### 計画・WBS生成（Planning）
- 利用者はAIを利用して，目標達成に必要なタスク群を自動生成できること．

#### 要求事項
- 入力された目標から，AIがメインタスクとサブタスク（WBS）を自動生成できること

- 各サブタスクに「想定工数（時間）」が割り振られること

- 生成された計画をプレビューし，どのタスクに対しても承認または再生成の指示ができること

#### 業務ルール
- ~~承認された計画は，初期バッファ100%（緑色）の状態で監視フェーズへ移行する~~ -> 計算式に基づくため．必ずしもそうなるとは限らない．

### タスク進捗・バッファ監視（Monitoring）
- 利用者はCCPMのロジックに基づき，プロジェクトの健全性をリアルタイムに把握できること．

#### 要求事項
- サブタスクの完了（チェック）操作ができること

- プロジェクトの進捗度を計算し，プログレスバー等で表示できること

- バッファ残存率に応じて，ステータス（Green/Yellow/Red）を視覚的に判定・表示できること

#### 業務ルール（バッファ計算の定義）
バッファ残存率は、初期の計画時点と現在時点の差分から動的に算出する。
*   $E_{\text{total}}$: 初期の生総工数
*   $T_{\text{init}}$: 計画時の残り時間
*   $E_{\text{remain}}$: 未完了タスクの生残工数合計
*   $T_{\text{current}}$: 現在時刻の残り時間

1.  **初期計画工数**: 生の総工数に安全係数を掛ける
    $$E_{\text{plan}} = E_{\text{total}} \times 1.2$$
2.  **初期バッファ**: 計画時の残り時間から初期計画工数を引く
    $$B_{\text{init}} = T_{\text{init}} - E_{\text{plan}}$$
3.  **現在のバッファ**: 現在の残り時間から、未完了タスクの残工数の1.2倍を引く
    $$B_{\text{current}} = T_{\text{current}} - (E_{\text{remain}} \times 1.2)$$
4.  **バッファ残存率 (%)**:
    $$\text{Rate} = \left( \frac{B_{\text{current}}}{B_{\text{init}}} \right) \times 100$$

- バッファ残存率50%以上をGreen（順調）、0%超〜50%未満をYellow（警告）、0%以下をRed（破綻の危機）とする。

### AIトリアージ＆サポート
利用者はバッファ状況に応じ，AIからのリスケジュールや要件削減の提案を受けられること．

#### 要求事項
- 選択したタスクの詳細とステータスを専用パネル（Right Panel）で確認できること

- Yellow状態のタスクに対し，要件の優先度分類（Must/Should/Could）をAIと相談できること

- Red状態のタスクに対し，AIから具体的な「要件の削減（Drop）」提案を受け取れること

- AIの提案を承認し，即座にタスク一覧（残工数）へ反映できること

- 変更履歴（タイムライン）を参照できること

#### 業務ルール
- 人間の感情（サンクコストと呼称することがある）による遅延を防ぐため，AIからのトリアージ提案は「削る前提」の強いトーンで行う

## 機能要件
### WBS（作業分解構成図）生成機能
#### 入力
- 最終目標（タスク名）

- 絶対期日

#### 出力
- メインタスクおよびサブタスクのリスト（JSON形式）

- 各サブタスクの想定工数

#### 処理概要
さくらのAI API（Sakura AI Engine）へプロンプトを送信し，タスクを分解して構造化データを返却する

### バッファ計算機能
#### 入力
- 現在日時

- 絶対期日（Deadline）

- 未完了サブタスクの想定工数合計

#### 出力
- バッファ残量（%）

- ステータス（Red/Yellow/Green）

#### 処理概要
残日数と残工数を比較し，プロジェクトの余裕度（バッファ）を算出してUIの色分けに反映する

### AIトリアージ提案機能
#### 入力
- 対象タスクの現状（目標，残り工数，現在のバッファ）

#### 出力
- 要件削減の具体案（例：「xxの分析を省き，yyのみに絞る」）

#### 処理概要
- バッファが枯渇（Red）した場合，AIに現状のタスク構造を送信し，削減可能なタスクを特定してユーザーへ提示する
- 
### 評価機能
#### 入力
- タスク終了の以下の観点でのユーザによる5段階評価
  1. トリアージの的確度
  2. 心理的救済度
  3. 実現可能性
- ユーザによる所感(任意)

#### 出力
- 次のAIのプロンプトの一部

#### 処理概要
- ユーザの評価をもとに各機能を最適化する．


## データ要件
### 管理対象データ
**メインタスク情報**: ID，タイトル，絶対期日，バッファ残量(%)

**サブタスク情報**: ID，タイトル，完了状態フラグ，想定工数(時間)

**履歴情報**: 変更日時，変更内容（削減された要件等）


### 外部インターフェース要件
#### フロントエンド連携
連携方式
Next.js API Routesを経由したHTTP通信

#### JSON形式データ交換

#### 外部API連携
連携先
さくらのAI API (Sakura AI Engine)

#### 処理内容
プロンプトの送信および生成結果（WBS，トリアージ提案）の受信